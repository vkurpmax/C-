#include <iostream>
using namespace std;

int main()
{
	int age = 23;
	int money = 4;
	if (age > 21 && money > 500)	{
		cout << "welcome" << endl;
	}
}
/*LOGICAL OPERATORS
Use "logical operators" to combine conditional statements and return "true" or "false".
&& AND operator
|| OR operator
! NOT operator

In the AND operator, both operands must be "true" for the entire expression to be "true".*/

/*QUIZ
Fill in the blanks to test both conditions in the following if statement.*/
