#include <iostream>
using namespace std;

int main()
{
	int age = 16;
	int score = 90;

	if (age > 20 || score > 50) {
		cout << "Accepted" << endl;
	}
	/* Outputs "Accepted"*/
}
/*THE OR OPERATOR
The OR (||) operator returns true if any one of its operands is "true".

You can combine any number or logical "OR" statements you want.
In addition, multiple "OR" and "AND" statements may be claimed together.*/
